#include <stdio.h>
#include <stdlib.h>

int main()
{
    char car;
    scanf(" %c", &car);
    printf("il primo carattere e`: %c\n", car);
    scanf(" %c", &car);
    printf("il secondo carattere e`: %c\n", car);

    return 0;
}
